query GetDataset($id: ID!) {
    dataset(id: $id) {
        ...Dataset
    }
}

query SearchDatasets($search: String, $values: [ID!]!) {
  options: datasets(
    filters: { search: $search, ids: $values }
    pagination: { limit: 10 }
  ) {
    value: id
    label: name
  }
}
