query GetBlock($id: ID!) {
    block(id: $id) {
        ...Block
    }
}

query SearchBlocks($search: String, $values: [ID!]) {
  options: blocks(
    filters: { search: $search, ids: $values }
    pagination: { limit: 10 }
  ) {
    value: id
    label: name
  }
}
