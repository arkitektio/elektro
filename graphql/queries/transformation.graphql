query GetTransformation($id: ID!) {
  transformation(id: $id) {
    ...Transformation
  }
}

# Unfiltered on this service: `transformations` takes no arguments. Ask
# `GetCoordinateGraph` for the edges around one space instead.
query GetTransformations {
  transformations {
    ...Transformation
  }
}
